# Claude Code 向け手順書：AI秘書＋Obsidian連携（AIの記憶フォルダ）をセットアップする

> この手順書は **Claude Code が読んで実行するためのもの** です。ユーザーと会話しながら、上から順に進めてください。
> 配布元（人間向けの解説ページ）：https://ryoya9595.github.io/obsidian-claude-setup/

---

## 0. ゴール

ユーザーが **いま Claude Code で開いている作業フォルダ** を、次の3つを兼ねる場所にする。

1. **Claude Code の記憶置き場** … セッションが変わっても、前回の続き・決めたこと・ミスを覚えている
2. **Obsidian の保管庫（Vault）** … 同じフォルダをObsidianで開けば、AIが書いた記憶を人間が読める・直せる
3. **AI秘書の事務所** … 「おはよう」で今日やることを優先順に出す／todo・メモ・1日のまとめを秘書が管理する。秘書の名前やキャラはユーザーが決める

仕組みはシンプル：このフォルダの `CLAUDE.md` に「毎回最初に記憶を読む」「節目で記憶に書く」「秘書として振る舞う」というルールを書く。Claude Code は作業フォルダの `CLAUDE.md` を毎回自動で読むので、次のセッションから記憶が働く。**MCPや追加のツールは使わない。**

完成形：

```
<作業フォルダ>/
├── CLAUDE.md          … Claude Code が毎回最初に読むルール（記憶の読み書き＋秘書の仕事）
├── README.md          … 人間向け：このフォルダの使い方
├── about-me.md        … ユーザーのプロフィール（呼び名・目標）
├── secretary.md       … AI秘書の設定（名前・性格・話し方）
├── todo.md            … やること（進行中／保留／完了。優先度つき）
├── daily/             … 日報（YYYY-MM-DD.md）。その日やったこと・決めたこと
├── projects/          … 取り組み中のことの「今どこ・次なに」
│   └── _INDEX.md
├── decisions/         … 大事な判断の記録
├── mistakes/          … ミスの記録（同じミスを繰り返さないため）
└── ideas/             … アイデアのメモ
```

---

## 1. 守ること（最優先）

- **ユーザーはパソコン初心者の可能性が高い。** 専門用語を避け、短く・やさしい日本語で話す。コマンドの中身を長々と説明しない。
- **触るのは「いま開いている作業フォルダの中」だけ。** 例外は「Obsidianが入っているかの確認（読むだけ）」と「`~/.claude/CLAUDE.md` の確認（読むだけ）」。付録Aはユーザーが頼んだときだけ。
- **既存のファイルは上書きしない。** 同じ名前のファイルがあれば中身を確認し、足りない部分だけ追記する（`CLAUDE.md` はセクションを末尾に追記）。
- **ファイルの作成・編集は、ファイル作成・編集用のツール（Write / Edit）で行う。** シェルのリダイレクト（`echo > file` など）は使わない。特に Windows の PowerShell はリダイレクトで文字化け（UTF-16）するため厳禁。文字コードは UTF-8。
- `.env`・パスワード・APIキーなどの認証情報のファイルは読まない。
- **作る前に「これから作るもの」を見せて、ユーザーのOKを1回もらう**（フェーズ2の最初）。
- 「できました」と言う前に、フェーズ4のチェックを実際に確認する。
- **この手順書そのものを作業フォルダに保存しない。** curl は画面に出力して読むか、一時フォルダ（Mac：`/tmp/obsidian-setup.md`／Windows：`$env:TEMP\obsidian-setup.md`）に保存する。作業フォルダに入ると Obsidian に表示されてしまう。

---

## フェーズ1：確認（読むだけ・何も変更しない）

次を確認する。

0. **作業フォルダに `setup.md`（この手順書）を保存してしまっていたら削除する**（この手順書と同じ内容であることを確認してから。ユーザーへの報告は不要）
1. **OS**（Mac / Windows）
2. **今の作業フォルダの場所（絶対パス）**
   - 作業フォルダが次のどれかなら **止まる**：ホームフォルダそのもの（`~` / `C:\Users\<名前>`）、デスクトップ・書類（Documents）・ダウンロードのフォルダそのもの、ドライブ直下、システムのフォルダ、一時フォルダ（`/tmp`・`/private/tmp`・`%TEMP%` など。再起動で消えるため）。
     → 理由：Obsidianがその中の全ファイルを読み込んでしまい、重くなる・関係ないファイルが混ざるため。
     → ユーザーへの案内：「記憶用の新しいフォルダ（例：書類の中に `Claude-Brain`）を作って、Claude Code でそのフォルダを開き直してから、もう一度同じメッセージを貼ってください」
   - OneDrive・iCloud Drive で同期されているフォルダ（Windows の「ドキュメント」は OneDrive 同期になっていることが多い）は **止まらない**。そのまま使えるが、フェーズ1の報告に「同期フォルダなので、2台のパソコンで同時に書き換えるとぶつかることがあります」と一言添える。
3. **フォルダの中身**
   - 空、またはほぼ空 → そのまま進む
   - すでにファイルがたくさんある（目安30個以上）→ 「このフォルダをそのまま記憶フォルダにすると、Obsidianに今のファイルも全部表示されます。このまま進めますか？それとも新しいフォルダを作りますか？」と聞く
   - `CLAUDE.md`・`daily/` などが既にある → どれがあるかを控えておき、フェーズ2で上書きしないようにする
   - `CLAUDE.md` に `# 記憶のルール（Obsidian連携）` が **すでに入っている**（この手順書の旧版で記憶フォルダを作った人）→ **「秘書だけ追加モード」** にする。フェーズ2は「2-1 の質問3・4」「`secretary.md` の作成」「`CLAUDE.md` への秘書ルールの追記」だけ行い、フェーズ3は飛ばしてフェーズ4へ。確認結果の報告で「記憶フォルダはもう入っているので、AI秘書だけ追加します」と伝える
   - `CLAUDE.md` に `<!-- ai-secretary:start -->` が **すでに入っている** → 秘書もセットアップ済み。「もう全部入っています」と伝え、フェーズ4-2 のテストだけ案内して終わる
4. **すでに別の「外部記憶」ルールが入っていないか**
   - `~/.claude/CLAUDE.md`（Windows は `%USERPROFILE%\.claude\CLAUDE.md`）があれば読み、**「セッションの最初に〇〇フォルダの記憶を読む」「日報を自動で記録する」のような記憶のルール**が書かれていないか確認する（単語が出てくるだけのものは対象外）
   - そういうルールが **ある場合だけ**、ユーザーに「以前に入れた記憶の仕組み（場所：〇〇）が見つかりました。今回はこのフォルダを記憶置き場にします。以前のものはそのまま残しておきます」と一言伝える（消さない・書き換えない）
5. **Obsidian が入っているか**
   - Mac：`/Applications/Obsidian.app` または `~/Applications/Obsidian.app`
   - Windows：`%LOCALAPPDATA%\Programs\Obsidian\Obsidian.exe` または `%LOCALAPPDATA%\Obsidian\Obsidian.exe`
   - 入っていなくても止まらない（フェーズ3で案内する）
6. **今日の日付**（システムの日付を使う。自分の知識で推測しない）

確認結果は、ユーザーに短く伝える。例：

> 確認できました！
> ・パソコン：Mac
> ・フォルダ：書類 ＞ Claude-Brain（空っぽ）
> ・Obsidian：まだ入っていません（あとで入れ方を案内します）
> （4 の「以前の記憶の仕組み」が見つかった場合だけ、ここに一言足す）

フォルダの場所は、長い絶対パスではなく「書類 ＞ Claude-Brain」のように分かりやすく言い換える。

---

## フェーズ2：記憶フォルダを作る

### 2-0. 作る前にOKをもらう

ユーザーに次のように見せて、OKをもらってから作る。

> このフォルダに、AIの記憶用のファイルとフォルダを作ります。
> （上の「完成形」のツリーを見せる）
> 既存のファイルは消しません。進めていいですか？

### 2-1. ユーザーに4つだけ質問する（about-me.md・secretary.md 用）

一度にまとめて聞く。「スキップ」「おまかせ」と言われた項目は下のとおり扱い、「あとで about-me.md／secretary.md を書き換えればOK」と伝える。

1. 秘書から何て呼ばれたいですか？（例：〇〇さん）　※スキップ → 「（未記入：あとで書いてOK）」とし、呼び方は「あなた」
2. いま取り組んでいること・これから目指していることは？（例：AI副業で月5万円／SNS発信を続けたい）　※スキップ → 「（未記入：あとで書いてOK）」
3. 秘書の名前は何にしますか？（例：ミナ／さくら／おまかせ）　※おまかせ → 呼びやすい2〜3文字の名前をAIが決める
4. 秘書のキャラ・話し方は？（例：やさしいお姉さん／元気な後輩／ていねいな執事／短く結論から）　※おまかせ → 「やさしく落ち着いた話し方。敬語すぎず、短く分かりやすく」

### 2-2. ファイルを作る

以下の中身で作る。`{…}` はその場の値に置き換える。既にあるファイルは上書きしない。

#### `CLAUDE.md`

中身は「記憶のルール」と「AI秘書のルール」の2ブロック。

- `CLAUDE.md` が無い → 2ブロックを続けて書いて新規作成する
- 既に `CLAUDE.md` がある → 2ブロックを **末尾に追記** する。記憶のルールの前後には `<!-- obsidian-memory:start -->` と `<!-- obsidian-memory:end -->` を付ける（同じ印か `# 記憶のルール（Obsidian連携）` がもうあれば、記憶のルールは追記しない）
- AI秘書のルールは、新規・追記どちらの場合も前後に `<!-- ai-secretary:start -->` と `<!-- ai-secretary:end -->` を付ける（同じ印がもうあれば追記しない）

**ブロック1：記憶のルール**

```markdown
# 記憶のルール（Obsidian連携）

このフォルダは、Claude Code の「記憶置き場」であり、Obsidian の保管庫でもある。
セッションが変わっても前回の続きから始められるように、次のルールを必ず守る。

## セッションの最初にやること（必須）
新しいセッションが始まったら、最初の返答の前に次を読む：
1. `about-me.md`（ユーザーのこと）
2. `todo.md`
3. `daily/` の新しい順に3件
4. `projects/_INDEX.md`

そして最初の返答で、前回の続き（最後にやったこと・残っているtodo）を2〜3行で伝える。
記録がまだ何もなければ、その旨を一言だけ伝える。
`secretary.md` があるときは、秘書のキャラで伝える。

## 記録するタイミング（ユーザーの許可は不要・自動で書く）
| タイミング | 書き込み先 |
|---|---|
| 何かが決まった・新しい情報が出た | `daily/YYYY-MM-DD.md` に追記 |
| 作業が終わった | `daily/YYYY-MM-DD.md` に追記、`todo.md` を更新 |
| 話題が変わった（区切り） | `daily/YYYY-MM-DD.md` に追記 |
| 「これ覚えておいて」と言われた | 内容に合う場所（`about-me.md`／`decisions/`／`daily/`）に書く |
| ミスを指摘された | `mistakes/YYYY-MM-DD-内容.md` を作る（何が起きた／原因／次からどうするか） |
| 大事な判断をした | `decisions/YYYY-MM-DD-内容.md` を作る（決めたこと／理由） |
| 新しい取り組みが始まった・状況が変わった | `projects/_INDEX.md` を更新 |

## 書き方のルール
- 日付はシステムの日付を使う（推測しない）
- `daily/` は上書きせず、末尾に追記する（その日のファイルがなければ作る）
- 1回の記録は短く、箇条書きで。あとで読み返して分かる書き方にする
- 書いたら、返答の最後に「（📝 記録しました）」と小さく添える
- ファイルは UTF-8 で、Write / Edit ツールで書く
- 書きすぎを恐れない。書き漏れより書きすぎの方がよい

## 記憶の整理（「記憶を整理して」と言われたら）
1. 1か月以上前の `daily/` を読み、まだ大事なこと（続いている取り組み・決まったこと）を `projects/_INDEX.md` や `decisions/` に移す
2. `todo.md` の完了済みを整理し、終わったものは「完了」に移す
3. `about-me.md` に、日報から分かったユーザーの好み・目標の変化を書き足す
4. 古い `daily/` は消さない（残したまま）。何を整理したかを今日の `daily/` に書く

## 書かないもの（大事）
- パスワード・APIキー・カード番号などの認証情報
- お客さんや他人の個人情報（名前・連絡先など）は、必要最低限にする

## ユーザーのこと
呼び方・目標は `about-me.md` に従う。
```

**ブロック2：AI秘書のルール**（新規でも追記でも、前後に印を付ける）

```markdown
<!-- ai-secretary:start -->
# AI秘書のルール

このフォルダのAIは、ユーザーの **AI秘書** として働く。
秘書の名前・性格・話し方・一人称は `secretary.md` に従う（ユーザーが書き換えたら、次の返答からそれに合わせる）。
セッションの最初に、記憶のルールで読むファイルに加えて `secretary.md` も読む。

## 秘書の仕事
| ユーザーの言葉（例） | やること |
|---|---|
| 「おはよう」「今日やること出して」 | **朝の段取り**：`todo.md`・直近の `daily/`・`projects/_INDEX.md` を読み、今日やることを **優先度の高い順に3つまで** 出す（期限が近いもの・前回やり残したものを先に）。それぞれ「なぜ今日か」を一言。最後に「どれからやる？」と聞く |
| 「todoに〇〇追加」「〇〇やらなきゃ」 | `todo.md` の「進行中」に追加。優先度（🔴高／🟡中／🟢低）と、期限があれば期限も書く。優先度が分からなければ1回だけ聞く |
| 「〇〇終わった」 | `todo.md` で「完了」に移し（日付つき）、`daily/` に記録。次にやることを1つ提案する |
| 「メモして」「アイデア思いついた」 | `ideas/` に1テーマ1ファイルで保存（同じテーマのファイルがあれば追記） |
| 「おつかれ」「今日のまとめ」 | 今日の `daily/` の末尾に「今日のまとめ」（やったこと／決めたこと／明日やること）を追記し、3行で報告 |
| 「今週の振り返り」 | 直近7日の `daily/` を読み、できたこと・残ったこと・来週の優先3つをまとめて、今日の `daily/` に追記 |

## 秘書としての心がけ
- 聞かれる前に、気づいたことを一言添える（期限が近いtodo、1週間以上止まっているtodoなど）。長くしない
- 提案は「〇〇をやる」まで具体的にする
- 急かしたり責めたりしない。終わったことはちゃんと一緒に喜ぶ
- 分からないことは推測で決めず、短く聞く
- 秘書のキャラで話しても、中身（事実・手順）は正確に

## 通常のClaudeの会話を引き継ぐとき
「通常のClaudeの会話も引き継いで」と頼まれたら、手順書 https://ryoya9595.github.io/obsidian-claude-setup/setup.md を curl（Windows は curl.exe）で取得して（このフォルダには保存しない）、「付録C」のとおりに進める。
<!-- ai-secretary:end -->
```

#### `README.md`

```markdown
# このフォルダについて

このフォルダは **Claude Code の記憶置き場** です。同時に **Obsidian の保管庫** でもあります。

- Claude Code がここに、日報・やること・決めたこと・ミスを自動で書きます
- Obsidian で開くと、AIが書いた記憶を読んだり、直したりできます
- 自分で書き足してもOK。AIは次のセッションでそれを読みます

## フォルダの役割
| 場所 | 中身 |
|---|---|
| `CLAUDE.md` | AIが毎回最初に読むルール（ここを書き換えるとAIの動きが変わる） |
| `about-me.md` | あなたのプロフィール（呼び方・目標） |
| `secretary.md` | AI秘書の設定（名前・性格・話し方）。書き換えるとキャラが変わります |
| `todo.md` | やること |
| `daily/` | 日報（1日1ファイル） |
| `projects/` | 取り組み中のことの「今どこ・次なに」 |
| `decisions/` | 大事な判断の記録 |
| `mistakes/` | ミスの記録（同じミスを繰り返さないため） |
| `ideas/` | アイデアのメモ |

## AI秘書への頼み方
| こう言う | 秘書がやること |
|---|---|
| 「おはよう」 | 今日やることを優先順に3つ出す |
| 「todoに〇〇追加」 | やることに追加（優先度・期限つき） |
| 「〇〇終わった」 | 完了にして、次にやることを提案 |
| 「メモして：〇〇」 | アイデアとして保存 |
| 「おつかれ」 | 今日のまとめと明日やることを記録 |
| 「今週の振り返り」 | 1週間分をまとめて、来週の優先3つを出す |

## 使い方のコツ
- Claude Code は **このフォルダを開いて** 使う（別のフォルダで開くと記憶を読みません）
- 「昨日の続きやろう」「今の状況教えて」で再開できます
- 覚えてほしいことは「これ覚えておいて」
- AIが間違えたら遠慮なく指摘する → `mistakes/` に残って、次から気をつけるようになります
- 月1回くらい「記憶を整理して」と頼むと、AIが古い日報をまとめてくれます
- パスワードやAPIキーは書かない・書かせない

セットアップ日：{今日の日付}
```

#### `about-me.md`

```markdown
# わたしについて

## 呼び方
{質問1の答え。スキップなら空欄}

## いま取り組んでいること・目指していること
{質問2の答え}

## そのほか覚えておいてほしいこと
（あとから自由に書き足してOK）
```

#### `secretary.md`

```markdown
# AI秘書の設定

ここを書き換えると、秘書のキャラが変わります（次の返答から反映されます）。

## 名前
{質問3の答え。おまかせならAIが決めた名前}

## 性格・話し方
{質問4の答え}

## 一人称
{キャラに合う一人称をAIが決める（例：わたし）}

## 口ぐせ・やってほしくないこと（任意）
（例：「絵文字は控えめに」「語尾に！を使う」「長文にしない」など。あとで自由に書き足してOK）
```

#### `todo.md`

```markdown
# やること

書き方：`- [ ] 🔴 〇〇（期限 10/5）`　優先度は 🔴高／🟡中／🟢低

## 進行中

## 保留・あとで

## 完了
- [x] AI秘書＋Obsidian連携をセットアップした（{今日の日付}）
```

#### `daily/{今日の日付}.md`

```markdown
# {今日の日付}

- AI秘書＋Obsidian連携（AIの記憶フォルダ）をセットアップした（秘書の名前：{秘書の名前}）
- 手順書：https://ryoya9595.github.io/obsidian-claude-setup/
```

#### `projects/_INDEX.md`

```markdown
# 取り組み中のこと

| 名前 | 今どこ | 次にやること |
|---|---|---|
| （まだなし） | | |
```

#### 空のフォルダ

`decisions/`・`mistakes/`・`ideas/` を作る。空のフォルダは Obsidian に表示されないことがあるので、それぞれに短い説明ファイルを1つ置く：
- `decisions/_このフォルダについて.md` … 「大事な判断を1件1ファイルで記録する場所」
- `mistakes/_このフォルダについて.md` … 「ミスを1件1ファイルで記録する場所。同じミスを繰り返さないため」
- `ideas/_このフォルダについて.md` … 「思いついたアイデアのメモ置き場」

### 2-3. 作ったものを確認する

ファイル一覧を表示して、完成形のツリーと一致しているか確認する。

---

## フェーズ3：Obsidian でこのフォルダを開く（ユーザーの手作業）

### 3-1. Obsidian が入っていない場合

- 案内：「https://obsidian.md/download からダウンロードして、インストールしてください。**アカウント登録は不要・無料** です」
  - Mac：ダウンロードした `.dmg` を開き、Obsidian を「アプリケーション」フォルダへドラッグ
  - Windows：ダウンロードした `.exe` を開くと自動でインストールされる
- 代わりに Claude Code がインストールすることもできる（**必ずユーザーに聞いてから**）：
  - Mac で Homebrew が入っている場合：`brew install --cask obsidian`
  - Windows で winget が使える場合：`winget install -e --id Obsidian.Obsidian`
  - どちらも無い／失敗した場合は、上の手動ダウンロードを案内する（無理に入れようとしない）
- ユーザーが「入れた」と言ったら次へ

### 3-2. 保管庫として開く

ユーザーに次の手順を案内する。フォルダの場所は「書類 ＞ Claude-Brain」のように言い換えて伝え、絶対パスも念のためコードブロックで添える。
パスが長い・迷いそうなときは、ユーザーに聞いてから Finder／エクスプローラーでこのフォルダの親フォルダを開いてあげる（Mac：`open "<親フォルダ>"`／Windows：`explorer "<親フォルダ>"`）。フォルダ選択の画面に、そのフォルダをドラッグ＆ドロップしても選べる。

1. Obsidian を起動する
2. 最初の画面で **「フォルダを保管庫として開く」**（英語表示なら **Open folder as vault**）の「開く」を押す
   - すでに別の保管庫を使っている人：左下の保管庫アイコン（または「保管庫を切り替える」）→「フォルダを保管庫として開く」
3. フォルダを選ぶ画面で、この作業フォルダを選んで「開く」
   - Mac のコツ：選ぶ画面で `Cmd + Shift + G` を押すとパスを貼り付けて移動できる
   - Windows のコツ：選ぶ画面の上のアドレス欄にパスを貼り付けて Enter
4. 「作成者を信頼してプラグインを有効化しますか？」のような確認が出たら **「信頼する」**（英語なら **Trust author and enable plugins**）。このフォルダにはプラグインは入っていないので安全
5. 左側に `daily`・`projects` などのフォルダが見えたら成功

---

## フェーズ4：動作確認

### 4-1. Claude Code 側の確認（Claude Code が自分でやる）

- [ ] 完成形のファイル・フォルダが全部ある
- [ ] `CLAUDE.md` に「記憶のルール」と「AI秘書のルール」が入っている（既存の内容は消えていない・同じルールが2回入っていない）
- [ ] `secretary.md` に秘書の名前とキャラが入っている
- [ ] `daily/{今日の日付}.md` にセットアップの記録がある
- [ ] ファイルが文字化けしていない（`CLAUDE.md` を読み直して日本語が正しく読める）

### 4-2. ユーザーに伝える「最後のテスト」

`CLAUDE.md` のルールは **次のセッションから** 効く。ユーザーに次を案内する。

1. Claude Code で **新しいセッション** を始める（**同じフォルダ** を選ぶこと）
2. こう送る：「今の状況を教えて」
3. 秘書が（決めたキャラで）「前回は AI秘書のセットアップをしました」のように **前回のことを話したら成功**
4. 続けて「todoに『Obsidianを眺めてみる』を追加して」と送る
5. 「おはよう」と送り、秘書が今日やることを出したら成功
6. Obsidian の画面で `todo.md` か今日の `daily` を開き、書き込まれていれば完璧

### 4-3. 完了報告

最後に、ユーザーへ次をまとめて伝える（短く）：
- 作ったもの（ツリー）
- 秘書の名前とキャラ（「これからよろしくね」を秘書のキャラで一言）
- 次にやること：上の「最後のテスト」
- 覚えておくこと3つ：
  - Claude Code は **このフォルダを開いて** 使う
  - 1日の始まりは「おはよう」、終わりは「おつかれ」
- 最後に一言：「いつも使っている通常のClaudeの会話も、『通常のClaudeの会話も引き継いで』と言えば秘書の記憶に引き継げます」
  - パスワードやAPIキーは記憶に書かせない

---

## 付録A：別のフォルダで作業するときも、この記憶を使いたい（ユーザーが頼んだときだけ）

通常は不要。ユーザーが「他のフォルダでも記憶を使いたい」と言ったときだけ行う。

1. 何をするか説明してOKをもらう：「Claude Code 全体の設定ファイル（`~/.claude/CLAUDE.md`）に、この記憶フォルダの場所を書き足します」
2. `~/.claude/CLAUDE.md`（Windows は `%USERPROFILE%\.claude\CLAUDE.md`）の末尾に次を追記する（ファイルが無ければ作る。既存の内容は消さない）：

```markdown
## 記憶フォルダ（Obsidian連携）
記憶フォルダの場所：{作業フォルダの絶対パス}
どのフォルダで作業するときも、セッションの最初にこの記憶フォルダの `CLAUDE.md` を読み、そこに書かれた「記憶のルール」に従って読み書きする。
```

3. ユーザーに伝える：「別のフォルダで使うと、記憶フォルダを読むときに許可を聞かれることがあります。そのときは許可してください」

## 付録C：通常のClaude（claude.ai・スマホアプリ）の会話を引き継ぐ（ユーザーが頼んだときだけ）

「通常のClaudeの会話も引き継いで」と頼まれたら行う。ユーザーの手作業は **エクスポートのボタンを押す・メールのリンクからダウンロードする** の2つだけにし、それ以外は Claude Code がやる。
Claude Code は claude.ai にログインできないので、エクスポートは必ずユーザーにやってもらう（代わりにブラウザを操作しない）。

### C-1. エクスポートを案内する
ユーザーに次を伝えて、「ダウンロードできた」の返事を待つ：
1. パソコンのブラウザで https://claude.ai を開く（スマホアプリではなくパソコンで）
2. 左下の自分の名前 →「設定」→「プライバシー」→「データをエクスポート」（英語表示なら Settings → Privacy → Export data）
3. しばらくするとメールが届くので、メールのリンクからzipをダウンロードする（リンクには期限があるので早めに）
4. ダウンロードしたzipは開かなくてOK。「ダウンロードできた」と送ってください

### C-2. zipを見つけて、記憶フォルダの外に展開する
- ダウンロードフォルダ（Mac：`~/Downloads`／Windows：`%USERPROFILE%\Downloads`）から、いちばん新しい `data-` で始まるzipを探す。見つからない・候補が複数あるときは、ファイル名を見せてユーザーに確認する
- **記憶フォルダの中には展開しない。** 一時フォルダ（Mac：`/tmp/claude-export`／Windows：`$env:TEMP\claude-export`）に展開する
  - Mac：`unzip -o <zip> -d /tmp/claude-export`／Windows：`Expand-Archive -Force <zip> $env:TEMP\claude-export`
- 中身の形は変わることがあるので決めつけない。まず会話が入っているファイル（通常は `conversations.json`）を見つけ、先頭だけ読んで構造を確かめてから進める
- 大きいファイルを一度に全部読まない。使えるもの（Mac：`python3`／Windows：PowerShell の `ConvertFrom-Json`）で必要な部分だけ取り出す。スクリプトを書く場合も一時フォルダに置く

### C-3. どれを引き継ぐか聞く
- 会話の一覧（番号・タイトル・最終更新日・やり取りの数）を新しい順に表示する（多いときは最新30件＋「ほかに〇件」）
- 「全部／最近3か月／番号で指定（例：1,3,5）」から選んでもらう。迷っていたら「最近3か月」をすすめる

### C-4. 読んで、要約して記憶に振り分ける
選ばれた会話を読み、**要約だけ** を記憶フォルダに書く（会話の生ログは書かない・コピーしない）。
| 見つけたもの | 書き込み先 |
|---|---|
| ユーザーの仕事・目標・好み・苦手なこと | `about-me.md` の「そのほか覚えておいてほしいこと」に追記 |
| 続いている取り組み（今どこ・次なに） | `projects/_INDEX.md` に行を追加 |
| はっきり決めたこと | `decisions/YYYY-MM-DD-内容.md`（日付は会話の日付） |
| アイデア・やりたいこと | `ideas/` |
| やり残していること | `todo.md` の「保留・あとで」 |
- 会話の途中の間違い・あとで変わった結論は書かない（最後の結論だけ）。古い会話と新しい会話で食い違うときは新しい方を採用する
- 「書かないもの」のルールを守る（パスワード・APIキー・カード番号は書かない。他人の個人情報は最小限）
- 今日の `daily/` に「通常のClaudeから〇件の会話を引き継いだ（期間：〜）」と記録する

### C-5. 後片付けと報告
- 一時フォルダの展開ファイルを削除する（Mac：`rm -rf /tmp/claude-export`／Windows：`Remove-Item -Recurse -Force $env:TEMP\claude-export`）
- ダウンロードしたzipは消さずに、ユーザーに「zipには全部の会話が入っています。人に渡さないでください。いらなければ削除してOKです」と伝える
- 何をどこに書いたかを短く報告し、「違うところがあれば直すので言ってください」と添える

## 付録B：うまくいかないとき（Claude Code 向け）

- **新しいセッションで前回のことを話さない** → 作業フォルダが違う可能性が高い。同じフォルダを開いているか確認してもらう。フォルダが合っているなら `CLAUDE.md` が作業フォルダの一番上の階層にあるか確認する
- **Obsidian に何も表示されない** → 別のフォルダを保管庫にしている。3-2 をやり直す（保管庫の切り替えから）
- **文字化けしている** → シェルのリダイレクトで書いた可能性。Write ツールで UTF-8 で書き直す
- **秘書のキャラが出ない・「おはよう」で段取りしない** → `CLAUDE.md` に「AI秘書のルール」があるか、`secretary.md` があるかを確認する
- **自動で記録してくれない** → 「CLAUDE.md の記憶のルールを読み直して、今の内容を daily に記録して」と頼んでもらう
